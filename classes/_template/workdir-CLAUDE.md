# <CODE> — <Class Name>

<!-- Template for the CLAUDE.md that goes in the class's workdir (not in this repo).
     Copy it to <workdir>/CLAUDE.md when adding a class, fill in the blanks, delete this
     comment. The shared system rules live in the semester folder's CLAUDE.md one level up
     (Coursework/<semester>/CLAUDE.md) — this file is only the class-specific part. -->

Class workdir for the course-brain system. The full rulebook is imported below (a synced
copy of the repo's CLAUDE.md — edit it in the repo, never here); the repo side of this
class is `classes/<code-lowercase>/` in your clone of the repo.

@_course-brain-rules.md

- Schedule: <schedule>. Role: <student | TA — if TA: assignment files in the repo are things to grade, not do; grading output goes in `grading/`>.
- Assignment notation: <the class's own naming, e.g. `L00`, `L01` or assignment slugs> —
  `homework/` gets one subfolder per assignment.
